name: Schedule Test

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Schedule
        run: |
          echo "Schedule works"
          date
